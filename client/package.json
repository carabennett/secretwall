{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.0",
    "gh-pages": "^6.3.0",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "boardgame.io": "^0.50.2"
  }
}
